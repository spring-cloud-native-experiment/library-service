spring:
  application:
    name: library-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka